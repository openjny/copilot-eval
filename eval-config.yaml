# yaml-language-server: $schema=./schemas/eval-config.schema.json
vars: {}

runner:
  epochs: 1
  timeout_seconds: 120
  container_image_base: copilot-eval
  otel_endpoint: http://host.docker.internal:4318

# For Azure Skills eval, use: --config-dir examples/azure-skills
